{
  "name": "a CI config file marks GITHUB relevant, and claims no capability",
  "$why": "evidenceRouting.ci maps to capability github; there is no ci capability. A config file cannot prove API access, so it raises github's scope questions as RELEVANCE without claiming the route \u2014 claiming it made discovery and verification disagree on the same machine.",
  "env": {
    "executables": [],
    "mcpServers": [],
    "repoFiles": [
      ".github/workflows/test.yml"
    ]
  },
  "expect": {
    "available": [],
    "unavailable": [
      "github",
      "infra",
      "logs",
      "metrics",
      "other"
    ],
    "relevant": [
      "github"
    ],
    "questions": [
      "github.repos",
      "github.subpaths",
      "github.baseBranch"
    ],
    "unassigned": []
  }
}
